#include <bits/stdc++.h> 
long long maxSubarraySum(int arr[], int n)
{
    int z=0;
    vector<int>a;
    int maxa=0;
    for(int i=0;i<n;i++){
        int sum=0;
        for(int j=i;j<n;j++){
            sum=sum+arr[j];
            maxa=max(maxa,sum);
        }
        
    }
    return maxa;
}
